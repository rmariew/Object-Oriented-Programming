Studio 20 ReadMe.txt
Regan Williamson
Bemi Folayan

2. We are supposed to delete the pointer to the file because if we don't it will be considered a dangling pointer.
4. We made a TextFile and a PasswordProxy of that file with the password "whispers". Through the PasswordProxy object, we called write, read, and 
accept(on both a BasicDisplayVisitor object and a MetadataVDisplayVisitor object). Each time one of these functions was called through the
PasswordProxy, the console prompts the user for the password and only goes through with the function call if the correct password is input.
